<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="1" author="jovichen">

        <!-- CUSTOMER -->
        <createTable tableName="customer">
            <column name="customer_id" type="SERIAL">
                <constraints primaryKey="true"/>
            </column>
            <column name="name" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="email" type="VARCHAR(100)">
                <constraints unique="true"/>
            </column>
        </createTable>

        <!-- BOOKING -->
        <createTable tableName="booking">
            <column name="booking_id" type="SERIAL">
                <constraints primaryKey="true"/>
            </column>
            <column name="customer_id" type="INT"/>
            <column name="room_id" type="INT"/>
            <column name="check_in" type="DATE"/>
            <column name="check_out" type="DATE"/>
        </createTable>

        <!-- SERVICE -->
        <createTable tableName="service">
            <column name="service_id" type="SERIAL">
                <constraints primaryKey="true"/>
            </column>
            <column name="service_name" type="VARCHAR(100)"/>
            <column name="price" type="NUMERIC(10,2)"/>
        </createTable>

        <!-- BOOKED SERVICE -->
        <createTable tableName="booked_service">
            <column name="booking_id" type="INT"/>
            <column name="service_id" type="INT"/>
            <column name="quantity" type="INT"/>
        </createTable>

        <!-- FOREIGN KEYS -->
        <addForeignKeyConstraint
            baseTableName="booking"
            baseColumnNames="customer_id"
            referencedTableName="customer"
            referencedColumnNames="customer_id"
            constraintName="fk_booking_customer"/>

        <addForeignKeyConstraint
            baseTableName="booked_service"
            baseColumnNames="booking_id"
            referencedTableName="booking"
            referencedColumnNames="booking_id"
            constraintName="fk_booked_booking"/>

        <addForeignKeyConstraint
            baseTableName="booked_service"
            baseColumnNames="service_id"
            referencedTableName="service"
            referencedColumnNames="service_id"
            constraintName="fk_booked_service"/>

    </changeSet>

</databaseChangeLog>